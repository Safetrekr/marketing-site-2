/**
 * Root Default Twitter Card Image
 *
 * Re-exports the OG image handler. Twitter `summary_large_image` cards
 * accept the standard 1200x630 OG dimensions.
 */

export { default, runtime, alt, size, contentType } from './opengraph-image'
